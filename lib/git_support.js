// Generated by CoffeeScript 1.4.0
var Shell, colors, defer, fs, git, mkdirp, nodefn, sequence;

Shell = require('./shell');

colors = require('colors');

fs = require('fs');

sequence = require('when/sequence');

nodefn = require('when/node/function');

defer = require('when').defer;

mkdirp = require('fs-extra').mkdirp;

module.exports = git = {
  getOrigin: function(workDir, callback) {
    var gitDir;
    gitDir = git.gitDir(workDir);
    try {
      return Shell.spawn('git', ["--git-dir=" + gitDir, 'config', '--get', 'remote.origin.url'], null, function(error, result) {
        if (error) {
          return callback(error);
        }
        return callback(null, result.stdout.trim());
      });
    } catch (error) {
      return callback(error);
    }
  },
  getHeadRef: function(workDir, callback) {
    var gitDir;
    gitDir = git.gitDir(workDir);
    return fs.readFile("" + gitDir + "/HEAD", function(error, data) {
      if (error) {
        return callback(error);
      }
      return callback(null, data.toString().match(/ref: (.*)\n$/)[1]);
    });
  },
  getHeadVersion: function(workDir, callback) {
    var gitDir;
    gitDir = git.gitDir(workDir);
    return git.getHeadRef(workDir, function(error, head) {
      if (error) {
        return callback(error);
      }
      return fs.readFile("" + gitDir + "/" + head, function(error, data) {
        if (error) {
          return callback(error);
        }
        return callback(error, data.toString().trim());
      });
    });
  },
  getStatus: function(workDir, callback) {
    var gitDir;
    gitDir = git.gitDir(workDir);
    return Shell.spawn('git', ["--git-dir=" + gitDir, "--work-tree=" + workDir, 'status'], null, callback);
  },
  gitDir: function(workDir) {
    return workDir + '/.git';
  },
  clone: function(workDir, origin, branch, masterDefer, callback) {
    var gitDir, tasks;
    gitDir = git.gitDir(workDir);
    tasks = sequence([
      function() {
        return nodefn.call(mkdirp, workDir);
      }, function() {
        var dfer;
        dfer = defer();
        if (Shell.gotDirectory(gitDir)) {
          dfer.reject('already cloned');
        } else {
          dfer.resolve();
        }
        return dfer.promise;
      }, function() {
        return nodefn.call(Shell.spawn, 'git', ['clone', origin, workDir], masterDefer);
      }
    ]);
    return tasks.then(function(result) {
      return callback(null, result);
    }, function(error) {
      if (error !== 'already cloned') {
        callback(error);
        return;
      }
      masterDefer.notify.info.good('already cloned', workDir);
      return callback(null, {});
    });
  }
};
